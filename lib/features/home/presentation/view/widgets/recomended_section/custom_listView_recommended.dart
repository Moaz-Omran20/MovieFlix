import 'package:flutter/material.dart';

import 'custom_listView_recommended_body.dart';

class CustomListViewRecommended extends StatelessWidget {
  const CustomListViewRecommended({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const CustomListViewRecommendedBody();
  }
}
